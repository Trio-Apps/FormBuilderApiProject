# دليل جميع العمليات والدوال المتاحة 📚

## 🔢 العمليات الحسابية الأساسية (6 عمليات)

| العملية | الرمز | المثال | النتيجة |
|---------|------|--------|---------|
| **الجمع** | `+` | `[A] + [B]` | 30 (إذا A=10, B=20) |
| **الطرح** | `-` | `[A] - [B]` | 10 (إذا A=30, B=20) |
| **الضرب** | `*` | `[QUANTITY] * [PRICE]` | 50 (إذا QUANTITY=5, PRICE=10) |
| **القسمة** | `/` | `[TOTAL] / [QUANTITY]` | 25 (إذا TOTAL=100, QUANTITY=4) |
| **باقي القسمة** | `%` | `[NUMBER] % 2` | 1 (إذا NUMBER=7) |
| **الأس** | `^` أو `**` | `[BASE] ^ 2` | 25 (إذا BASE=5) |

---

## 📊 دوال الإحصاء والجمع (4 دوال)

### 1. SUM - جمع القيم
- **Syntax:** `SUM(value1, value2, value3, ...)`
- **يدعم:** أي عدد من المعاملات (2، 3، 4، 5، ...)
- **مثال:** `SUM([A], [B], [C])` → 60 (إذا A=10, B=20, C=30)

### 2. AVG / AVERAGE - متوسط القيم
- **Syntax:** `AVG(value1, value2, ...)` أو `AVERAGE(value1, value2, ...)`
- **يدعم:** أي عدد من المعاملات
- **مثال:** `AVG([A], [B], [C])` → 20 (إذا A=10, B=20, C=30)

### 3. MAX - القيمة العظمى
- **Syntax:** `MAX(value1, value2, ...)`
- **يدعم:** أي عدد من المعاملات (2، 3، 4، 5، ...)
- **مثال:** `MAX([N1], [N2], [N3])` → 23 (إذا N1=1, N2=23, N3=3)
- **مثال:** `MAX([N1], [N2], [N3], [N4])` → 50 (إذا N1=1, N2=23, N3=3, N4=50)

### 4. MIN - القيمة الصغرى
- **Syntax:** `MIN(value1, value2, ...)`
- **يدعم:** أي عدد من المعاملات (2، 3، 4، 5، ...)
- **مثال:** `MIN([N1], [N2], [N3])` → 1 (إذا N1=1, N2=23, N3=3)

---

## 🔬 دوال الرياضيات المتقدمة (7 دوال)

### 5. SQRT - الجذر التربيعي
- **Syntax:** `SQRT(value)`
- **مثال:** `SQRT([AREA])` → 4 (إذا AREA=16)

### 6. ABS - القيمة المطلقة
- **Syntax:** `ABS(value)`
- **مثال:** `ABS([DIFFERENCE])` → 10 (إذا DIFFERENCE=-10)

### 7. ROUND - التقريب
- **Syntax:** `ROUND(value)` أو `ROUND(value, decimals)`
- **مثال:** `ROUND([VALUE], 2)` → 3.46 (إذا VALUE=3.456)

### 8. FLOOR - التقريب للأسفل
- **Syntax:** `FLOOR(value)`
- **مثال:** `FLOOR([VALUE])` → 3 (إذا VALUE=3.7)

### 9. CEIL / CEILING - التقريب للأعلى
- **Syntax:** `CEIL(value)` أو `CEILING(value)`
- **مثال:** `CEIL([VALUE])` → 4 (إذا VALUE=3.2)

### 10. POW - الأس
- **Syntax:** `POW(base, exponent)`
- **مثال:** `POW([BASE], [EXPONENT])` → 8 (إذا BASE=2, EXPONENT=3)

### 11. MOD - باقي القسمة
- **Syntax:** `MOD(value1, value2)`
- **مثال:** `MOD([NUMBER], [DIVISOR])` → 1 (إذا NUMBER=10, DIVISOR=3)

---

## 📋 ملخص سريع

| الدالة | Syntax | عدد المعاملات | مثال |
|--------|--------|----------------|------|
| SUM | `SUM(v1, v2, ...)` | غير محدود | `SUM([A], [B], [C])` |
| AVG | `AVG(v1, v2, ...)` | غير محدود | `AVG([A], [B], [C])` |
| MAX | `MAX(v1, v2, ...)` | 2+ | `MAX([N1], [N2], [N3])` |
| MIN | `MIN(v1, v2, ...)` | 2+ | `MIN([N1], [N2], [N3])` |
| SQRT | `SQRT(value)` | 1 | `SQRT([AREA])` |
| ABS | `ABS(value)` | 1 | `ABS([DIFF])` |
| ROUND | `ROUND(value, decimals?)` | 1-2 | `ROUND([V], 2)` |
| FLOOR | `FLOOR(value)` | 1 | `FLOOR([V])` |
| CEIL | `CEIL(value)` | 1 | `CEIL([V])` |
| POW | `POW(base, exp)` | 2 | `POW([B], [E])` |
| MOD | `MOD(v1, v2)` | 2 | `MOD([N], [D])` |

---

## 🎯 أمثلة على التعبيرات المعقدة

```
ROUND(SUM([A], [B], [C]) / 3, 2)
MAX([A], [B], [C]) - MIN([A], [B], [C])
SQRT(POW([BASE], 2))
```

---

## ✅ ملاحظات مهمة

1. ✅ **جميع الدوال تدعم أي عدد من المعاملات** (2، 3، 4، 5، ...)
2. ✅ **MAX و MIN تدعم أي عدد من المعاملات**
3. ✅ **القيم المفقودة تصبح 0 تلقائياً**
4. ✅ **يمكن استخدام الأقواس `()` لتحديد أولوية العمليات**
5. ✅ **يمكن دمج عدة دوال في تعبير واحد**

---

## 🚀 API Endpoint

```
POST /api/Formulas/calculate-safe
```

**Request:**
```json
{
  "expressionText": "MAX([N1], [N2], [N3])",
  "fieldValues": {
    "N1": 1,
    "N2": 23,
    "N3": 3
  }
}
```

**Response:**
```json
{
  "success": true,
  "data": 23,
  "statusCode": 200
}
```

