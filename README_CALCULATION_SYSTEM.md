# نظام الحساب - Calculation System 📊

## ✅ المشروع جاهز للتشغيل!

تم بناء المشروع بنجاح. يمكنك تشغيله الآن.

## 🚀 كيفية التشغيل

### 1. تشغيل Backend API
```bash
cd frombuilderApiProject
dotnet run
```

أو من Visual Studio:
- اضغط F5 أو Run

### 2. الوصول إلى Swagger
بعد التشغيل، افتح المتصفح على:
```
https://localhost:7276/swagger
```

## 📋 الملفات التوثيقية المتاحة

### في مجلد `Documentation/`:
1. **CALCULATION_OPERATIONS.md** - دليل شامل بجميع العمليات والدوال (17 عملية)
2. **CALCULATION_EXAMPLES.json** - أمثلة جاهزة بصيغة JSON
3. **ANGULAR_INTEGRATION.md** - دليل تكامل Angular

### في مجلد `frombuilderApiProject/`:
- **API_CALCULATION_TESTS.http** - أمثلة لاختبار API

## 🔢 العمليات المتاحة

### العمليات الأساسية (6):
- `+` - الجمع
- `-` - الطرح
- `*` - الضرب
- `/` - القسمة
- `%` - باقي القسمة
- `^` أو `**` - الأس

### دوال الإحصاء (4):
- `SUM(...)` - جمع القيم
- `AVG(...)` - متوسط القيم
- `MAX(...)` - القيمة العظمى
- `MIN(...)` - القيمة الصغرى

### دوال الرياضيات المتقدمة (7):
- `SQRT(value)` - الجذر التربيعي
- `ABS(value)` - القيمة المطلقة
- `ROUND(value, decimals?)` - التقريب
- `FLOOR(value)` - التقريب للأسفل
- `CEIL(value)` - التقريب للأعلى
- `POW(base, exponent)` - الأس
- `MOD(value1, value2)` - باقي القسمة

## 🎯 API Endpoint الرئيسي

```
POST /api/Formulas/calculate-safe
```

**Request:**
```json
{
  "expressionText": "MAX([N1], [N2], [N3])",
  "fieldValues": {
    "N1": 1,
    "N2": 23,
    "N3": 3
  }
}
```

**Response:**
```json
{
  "success": true,
  "data": 23,
  "statusCode": 200
}
```

## ✅ ملاحظات مهمة

1. ✅ جميع الدوال تدعم أي عدد من المعاملات (2، 3، 4، 5، ...)
2. ✅ MAX و MIN تدعم أي عدد من المعاملات
3. ✅ القيم المفقودة تصبح 0 تلقائياً
4. ✅ يمكن استخدام الأقواس `()` لتحديد أولوية العمليات
5. ✅ يمكن دمج عدة دوال في تعبير واحد

## 📚 للمزيد من المعلومات

راجع الملفات في مجلد `Documentation/`:
- `CALCULATION_OPERATIONS.md` - دليل شامل
- `CALCULATION_EXAMPLES.json` - أمثلة جاهزة
- `ANGULAR_INTEGRATION.md` - دليل Angular

---

**جاهز للاستخدام!** 🎉

